// A web page as reading matter — the shared TypeScript, so the crawl and the
// scheduled refresh read a page the same way and hash the same text.
export { htmlToText, pageLinks, pageText } from '../../../supabase/functions/_shared/library/html.ts';
